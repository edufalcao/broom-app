## Broom v1.5.0 - Project Artifacts, Installers, and Honest Uninstalls

**Broom 1.5.0** adds per-project build-artifact cleanup and an installer-file sweep — both adopted clean-room from the Mole project's concepts — and makes every uninstall preview match exactly what gets deleted.

---

### Project Artifacts
- New Artifacts section finds regenerable build outputs (node_modules, target, .build, Pods, and more) grouped by project
- Active projects are detected by modification time and start deselected; a final re-check before deletion drops anything that went active since the scan
- Configurable search roots in Settings, plus support for CACHEDIR.TAG-marked cache directories

### Installers
- New Installers mode in Large Files reclaims leftover disk images, packages, and app-bearing ZIP archives from Downloads, Desktop, Documents, and the Homebrew download cache
- Mounted disk images are never offered, and freshly downloaded files are protected by an adjustable age gate

### Trust
- The uninstall detail view, confirmation sheet, and executed file set are now always identical — planner-discovered files appear up front and stay editable
- Group checkboxes set an explicit selection state instead of toggling every row
- The completion screen stays until dismissed instead of disappearing on its own

### Quality
- 206 tests across 32 suites, including UI smoke tests for the new sections

### Install
Download **Broom-v1.5.0.dmg** below, open it, and drag Broom to your Applications folder.
